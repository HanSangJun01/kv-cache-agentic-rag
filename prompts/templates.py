"""LLM 프롬프트 템플릿. (G) = Generator, (J) = Judge 모델이 사용."""

# ---------------------------------------------------------------- Agentic RAG (rag/pipeline.py)
GRADE_PROMPT = """(J) You grade retrieved passages for a question about the technology "{name}".
A passage is relevant only if it contains concrete information that helps answer the question
about {name} itself (not only about a baseline, prior work, or unrelated component).

Question: {question}

Passages:
{docs}

Return the ids of relevant passages (empty list if none)."""

REWRITE_PROMPT = """(G) The search for the question below returned no relevant passages from the paper about "{name}".
Rewrite it as a short English keyword query (technical terms, metric names, synonyms) likely to match the paper text.
Return only the query.

Question: {question}"""

# ---------------------------------------------------------------- 기술 조사 · 평가 (agents/research.py)
COMMON_RULES = """규칙:
- 제공된 근거만 사용한다. 근거에 없는 사실·수치를 만들지 않는다.
- 모든 사실·수치 문장 끝에 근거의 출처 태그를 그대로 붙인다. 논문: [P-SW p.N] / [P-HW p.N], 웹: [WM1] / [WD1] 형식. 근거 목록에 없는 태그는 쓰지 않는다.
- 수치는 원문 표기 그대로 옮긴다(반올림·단위 변환 금지). 수치 문장의 태그는 그 수치가 실제로 적힌 페이지여야 한다.
- 논문 수치는 "저자 보고 기준"임을 드러낸다. baseline·관련 연구에 대한 서술을 대상 기술의 성과로 옮기지 않는다.
- 근거에 없는 정성적 수식어("폭발적", "매우 유리", "극대화" 등)나 해석적 결론을 덧붙이지 않는다.
- 한국어로 작성하되 기술 용어는 영문 병기 가능."""

PROFILE_PROMPT = """(G) 너는 기술 조사 에이전트다. 아래 논문 근거만으로 "{name}"의 TechProfile 을 작성하라.
항목: 개요, 핵심 방식, 저자 보고 성과(수치·조건 포함), 실험 조건(모델·HW·baseline), 한계(저자가 밝힌 오버헤드·전제), 도입 조건.
{rules}

근거:
{evidence}"""

EVAL_PROMPT = """(G) 너는 {perspective_name} 평가 에이전트다. 평가 도메인: {domain}.
기술 "{name}" ({camp}) 을 아래 기준으로 평가하라. 등급은 "이 관점에서 유리하게 인식되는 정도"이지 기술 간 우열이 아니다.

평가 기준·루브릭:
{criteria}

등급 규칙:
- rating 은 높음/중간/낮음/판단 유보. 근거가 부족하면 반드시 "판단 유보".
- confidence(높음/중간/낮음) 는 근거의 양과 독립성(제3자 자료인지, 저자 자체 실험인지)으로 정한다.
- supporting_evidence 와 counter_evidence(반대·한계·리스크)를 모두 채운다. 반대 근거가 없으면 "확인된 반대 근거 없음" 이라고 쓴다.
- 대상 기술 자체가 아닌 인접 기술·일반 시장 자료는 "간접 근거"라고 표시한다. 일반 시장의 성장을 그 기술의 시장성으로 단정하지 않는다.
- 시장조사기관 리포트 수치는 기관별 방법론·편차가 크므로, 독립 출처가 여럿 일치하지 않으면 confidence 를 "중간" 이하로 둔다.
- 웹 자료 중 경쟁사 반응·개발자 의견·투자/애널리스트 평가 같은 반응·의견은 등급 근거로 쓰지 않고, 그 안의 측정 사실만 인용한다.
- D3(품질 유지)는 정확도·벤치마크 점수만 근거로 인정한다. 처리량·지연 수치를 품질 근거로 쓰지 않는다. KV 를 압축·근사하지 않고 위치만 옮기는 구조(메모리 계층 확장·오프로딩)라면 "설계상 손실 요인 없음(측정값 아님)"으로 명시하고, 정확도 측정이 없으면 rating 은 "판단 유보"로 둔다.
- overall_view: 이 관점에서 이 기술이 어떻게 인식되는지 2~3문장. information_gaps: 확인하지 못한 정보.
{rules}

기술 프로필(기술 조사 에이전트 결과):
{profile}

근거 (기준별):
{evidence}"""

REFLECT_PROMPT = """(J) 너는 근거 검증자다. 아래 "{name}" 에 대한 결과물의 각 주장이 인용 태그가 가리키는 원문으로 뒷받침되는지 검사하라.
검사: (1) 수치가 해당 페이지/자료에 실제로 있는가 (2) 지표·조건(모델, 배치, 문맥 길이, 비교 대상)이 원문과 같은가
(3) 대상 기술에 대한 서술인가 (baseline·관련 연구의 서술을 대상 기술로 옮기지 않았는가) (4) 인용 없이 사실을 단정하지 않았는가
(5) 평가 결과라면 rating 이 아래 루브릭 정의와 맞는가 (예: 신규 인프라·장비가 필요한데 도입 용이성을 "중간" 이상으로 둠).
문제가 있는 주장만 구체적으로(어떤 문장, 무엇이 틀렸는지) issues 에 적어라. 사소한 표현 차이는 무시한다. 문제가 없으면 빈 리스트.

{rubric}
인용된 원문:
{sources}

결과물(JSON):
{output}"""

FIX_PROMPT = """(G) 아래 결과물에서 검증자가 지적한 문제만 고쳐 같은 스키마로 다시 작성하라.
근거로 확인되지 않는 주장은 삭제하거나 "판단 유보"/정보 공백으로 옮긴다. 나머지 내용과 출처 태그는 유지한다.
{rules}

지적 사항:
{issues}

근거:
{evidence}

원래 결과물(JSON):
{output}"""

# ---------------------------------------------------------------- 종합 · 보고서 · 검토 (agents/report.py)
SYNTHESIS_PROMPT = """(G) 너는 평가 종합 에이전트다. 시장성 관점과 도메인 적용 관점({domain}) 평가 결과를 종합하라.
- 새 사실·수치를 만들지 않는다. 입력에 있는 출처 태그([P-SW p.N], [WM1] 등)를 그대로 유지한다.
- agreements: 두 관점이 같은 방향으로 보는 지점.
- conflicts: 두 관점이 엇갈리는 지점. 각 항목에 주제, 대상(SW/HW/공통), 시장 관점, 도메인 관점, 엇갈리는 이유. 최소 3개.
- tech_contrast: 같은 관점 안에서 두 기술이 다르게 인식되는 지점.
- complementarity: SW(데이터를 작게)·HW(공간을 넓게) 병행·보완 가능성. 근거가 있을 때만, 추천이 아닌 사실 서술로.
- caveats: 저자 자체 실험 의존, 정보 비대칭, 판단 유보 항목 등 해석상 유의점.
- 우열·순위·추천 판단을 하지 않는다.

기술 프로필:
{profiles}

시장성 평가:
{market}

도메인 평가:
{domain_eval}"""

REPORT_PROMPT = """(G) 너는 보고서 생성 에이전트다. 아래 입력만으로 "KV cache 최적화 기술 다관점 평가 보고서" 본문을 마크다운으로 작성하라.

목적: 두 기술의 우열을 판정하거나 추천하지 않고, 시장성 관점과 도메인 적용 관점에서 각 기술이 어떻게 다르게 인식되는지 비교·대조한다.

목차 (이 순서, 이 제목, 모두 "## " 2단계 제목. 하위 제목은 "### "):
## SUMMARY
## 1. 분석 배경
## 2. 기술 선정
## 3. 기술 개요
## 4. 관점별 평가
## 5. 시사점
## 6. 한계점
(REFERENCE 는 쓰지 않는다. 시스템이 인용 태그로 자동 생성한다.)

장별 지시:
- SUMMARY: 개요가 아니라 평가 결과 핵심 요약. 관점별 핵심 판정과 가장 큰 상충 지점을 담는다. 공백 포함 {summary_max}자 이내(엄수), 3~5개 불릿.
- 1장: KV cache 가 연산 병목을 메모리(HBM 용량) 병목으로 바꾼 배경, SW(데이터를 작게) vs HW(공간을 넓게) 두 진영, 둘이 병행될 수 있다는 점, 평가 도메인({domain})과 범위(시장성·도메인 적용 2개 관점, 기술성숙도·이해관계자 관점 제외).
- 2장: 반드시 다음 문장을 포함: "{selection_sentence}" 이어서 기술별 선정 사유.
- 3장: 기술별 핵심 방식, 저자 보고 성과, 한계. 수치는 "저자 보고 기준"으로 표기.
- 4장: "### 4.1 시장성 관점", "### 4.2 도메인 적용 관점"만 둔다. 각 절에 아래 등급 표를 그대로 넣고, 기준별로 두 기술이 어떻게 인식되는지 근거·반대 근거를 함께 서술한다. 이해관계자 의견(경쟁사 반응·개발자 평가·투자 시각)은 근거로 쓰지 않는다.
- 5장: 관점에 따라 평가가 엇갈리는 부분 중심. "| 주제 | 대상 | 시장 관점 | 도메인 관점 | 엇갈리는 이유 |" 표를 포함하고, 기술 간 인식 차이와 SW·HW 보완 가능성을 정리. 두 논문이 결합을 직접 실험하지 않았다면 결합 효과는 "검증되지 않은 가능성"으로만 쓰고, 인용 태그는 각 기술에 대한 사실 부분에만 붙인다.
- 6장: 공개 정보 기반 추정의 한계(저자 자체 실험 의존, 시장 자료의 간접성, 판단 유보 항목 등)와 아래 확증편향 방지 조치 7가지를 모두 서술.

작성 규칙:
- 분량: 본문 전체 7,000자 안팎(A4 10쪽 이내). 표는 간결하게.
- 모든 사실·수치 문장 끝에 입력에 있는 출처 태그를 그대로 붙인다: 논문 [P-SW p.N] / [P-HW p.N], 웹 [WM1] / [WD2]. 태그 형식을 바꾸거나 합치지 않는다([P-SW p.3, p.5] 금지 → [P-SW p.3][P-SW p.5]). 사용 가능 태그 목록 밖의 태그는 쓰지 않는다.
- 수치는 입력의 표기 그대로 쓰고, 수치 바로 뒤의 태그는 그 수치가 있는 페이지여야 한다. 표 안에는 수치를 넣지 말고 본문에서 인용한다.
- 다음 표현을 쓰지 않는다(인용·설명 목적 포함): {forbidden}. "A가 B보다 낫다" 식 비교, 순위, 추천·권고를 하지 않는다.
- 평가 등급은 입력 평가 결과와 다르게 바꾸지 않는다.

평가 도메인: {domain}
선정 기술:
{technologies}

기술 프로필:
{profiles}

시장성 평가:
{market}

도메인 평가:
{domain_eval}

평가 종합:
{synthesis}

4.1 등급 표:
{market_table}

4.2 등급 표:
{domain_table}

확증편향 방지 조치(6장):
{bias}

사용 가능 출처 태그:
{source_tags}"""

REVISE_PROMPT = """

---
[재작성 지시] 이전 초안이 검토에서 아래 문제로 반려됐다. 지적된 부분만 고치고 나머지 구조·내용·출처 태그는 유지해 전체 본문을 다시 출력하라.

critical:
{critical}

minor(가능하면 반영):
{minor}

이전 초안:
{draft}"""

REVIEW_PROMPT = """(J) 너는 보고서 검토자(LLM-as-a-Judge)다. 아래 보고서를 평가 결과(원자료)와 대조해 검토하라.

critical 로 분류하는 경우만(그 외는 minor):
1. 특정 기술을 추천하거나 우열·순위를 판정하는 서술이 있음
2. 시장 관점과 도메인 관점이 엇갈리는 지점이 명시적으로 드러나지 않음 (5장 상충 표·서술 부재)
3. 수치에 출처가 없거나, 인용된 출처가 주장을 명백히 뒷받침하지 않음(원자료와 모순)
4. 논문 수치를 "저자 보고 기준"으로 구분하지 않고 제3자 검증된 사실처럼 단정함
5. SUMMARY 가 결과 요약이 아니라 목차·개요 소개에 그침
6. 이해관계자 의견(경쟁사 반응, 개발자 평가, 투자/애널리스트 시각)이나 기술성숙도(TRL) 평가를 근거로 사용함
7. 평가 등급이 원자료와 다름

각 항목은 위치(장)와 문제를 한 문장으로 적어라. 문제가 없으면 빈 리스트.

원자료(평가 결과 JSON):
{evidence}

보고서:
{report}"""
